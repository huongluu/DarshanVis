{
    "_comment": "For Top K apps, should be able to choose chart type",
    "id": 9,
    "name": "top-k-app-chart",
    "type": "boxplot",
    "title": "Top 15 Big Time Application",
    "subtitle": "",
    "xAxis": {
        "title": "Application",
        "attribute": "appname"
    },
    "yAxis": {
        "title": "I/O Throughput"
    },
    "tooltip": {
        "valueSuffix": ""
    },
    "series": [
        {
            "type": "boxplot", 
            "name": "",
            "query": "select appname, sum(runtime)/3600 as total_runtime_h, sum(iotime)/3600 as total_iotime_h, sum(total_bytes)/(1024*1024*1024*1024) as total_bytes_TB, count(*) as numjobs, avg(iotime/runtime) as avg_io_percentage, avg(agg_perf_MB/1024) as avg_perf_GB from jobs_info where agg_perf_MB is not null group by appname order by total_iotime_h desc limit 15;",
            "appname": "appname",
            "total_runtime_h": "total_runtime_h",
            "total_iotime_h": "total_iotime_h",
            "total_bytes_TB": "total_bytes_TB",
            "numjobs": "numjobs",
            "avg_io_percentage": "avg_io_percentage",
            "avg_perf_GB": "avg_perf_GB"

        }
    ]
}
