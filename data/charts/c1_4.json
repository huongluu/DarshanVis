{
    "id": 9,
    "name": "top-k-app-chart",
    "type": "boxplot",
    "title": "Top 15 Big Time Application",
    "subtitle": "",
    "xAxis": {
        "title": "Application",
        "attribute": "real_exe"
    },
    "yAxis": {
        "title": "I/O Throughput"
    },
    "tooltip": {
        "valueSuffix": ""
    },
    "series": [
        {
            "type": "boxplot",
            "name": "",
            "query": "select real_exe, min(agg_perf_MB) as min_jobsize, avg(agg_perf_MB) as avg_jobsize, max(agg_perf_MB) as max_jobsize, sum(total_bytes) as total_bytes, sum(runtime) as runtime, sum(iotime) as iotime from jobs_all group by real_exe order by max_jobsize desc limit 15;",
            "min": "min_jobsize",
            "q1": "min_jobsize",
            "median": "avg_jobsize",
            "q3": "max_jobsize",
            "max": "max_jobsize"
        }
    ]
}